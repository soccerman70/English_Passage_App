# 어휘 심화 시험지 제작 명세 (앱 내장판)

정본은 **`public/vocabulary_test_sample.pdf`** 다. 원본 워크플로우 명세서 v1.0 은 참고 자료이며,
문면이 샘플과 어긋나면 **샘플을 따른다**. 사람이 대화로 승인하던 절차는 앱에서 쓰지 않으므로 뺐다.

---

## 1. 입력 — 더 받지 않는다

두 가지가 이미 앱 안에 있다. 원문 파일을 다시 올리는 절차는 없다.

| 필요한 것 | 이 앱에서 |
| --- | --- |
| 단어장 | `rows` — 3단계에서 만들고 ④로 확정한 것 |
| 원본 지문 | `passages` — 1단계에서 넣고 분할한 것 |
| 시험지 제목 | `docTitle` — 최대 30자 |

### PART IV 문장 조달

표제어를 고르는 순간 그 문장이 `rows[].sentence` 에 담긴다. **이것을 그대로 쓴다.**

맥락이 모자라 앞뒤 문장을 붙여야 할 때만 지문에서 가져온다.

```
passages.find(p => p.no === row.passageNo).english
  → splitSentences(...)          // 이미 있는 함수, 약어(Dr., e.g.) 보호됨
  → row.sentence 앞/뒤 한 문장을 이어붙인다
```

- 유추가 가능하면 붙이지 않는다
- 붙여도 한 문항 **40단어 이내**
- 지문 원문 그대로 (학생이 학습한 자료와 일치해야 한다)

---

## 2. 데이터 매핑

| 시험지에 필요한 것 | `rows` 필드 |
| --- | --- |
| 단어 | `headword` (원형으로 정규화됨) |
| 품사 | `pos` (한 글자: 명·동·형·부·전·접·대·구) |
| 뜻 | `meaning` |
| 파생어 | `derivatives` `[{word, pos}]` 최대 2 |
| 유의어 | `synonyms` `[{word}]` 최대 2 |
| 반의어 | `antonyms` `[{word, confidence}]` 최대 2 |
| 출처 | `passageNo` |

명세서에 없지만 이 앱에만 있는 것 — PART IV 에 결정적이다.

| 필드 | 쓰임 |
| --- | --- |
| `surface` | **지문에 나온 그대로의 형태.** `headword`(원형)와 비교하면 무엇이 어떻게 변형됐는지 드러난다 |
| `sentence` | 출처 문장 |
| `properNoun` | 고유명사 표시 — 출제에서 뺀다 |

---

## 3. 시험지 형식 (샘플 그대로)

### 3.1 머리글·꼬리글
```
{docTitle}                                    이름: ____  학번: ____
                      VOCABULARY TEST
                          …
                    — 수고하셨습니다 —
```
`VOCABULARY TEST` 와 이름·학번 칸은 고정. 슬로건은 넣지 않는다.

### 3.2 지시문 (문구 그대로)
```
PART I    우리말에 해당하는 영어 단어 또는 어구를 쓰시오.
PART II   다음 영영풀이에 해당하는 단어를 보기에서 고르시오.
PART III  주어진 두 단어의 관계와 같도록 빈칸에 알맞은 단어를 쓰시오.
PART IV   문장의 빈칸에 들어갈 동사를 보기에서 골라 바른 형태로 쓰시오.
PART V    빈칸에 들어가기에 적절한 단어를 모두 고르시오.
PART VI   글의 빈칸에 들어갈 낱말을 보기에서 고르시오.
```

보기 줄은 `보기 • 단어 • 단어 …` 형식.

### 3.3 구성
| PART | 문항 | 번호 | 보기 | 쪽 |
| --- | --- | --- | --- | --- |
| I | 10 | 1~10 | — | 1 |
| II | 5 | 11~15 | 6개 (distractor 1) | 1 |
| III | 5 | 16~20 | — | 1 |
| IV | 5 | 21~25 | 5개 (distractor 없음) | 1 |
| V | 5 | 26~30 | 5지선다 | 2 |
| VI | 5 | 31~35 | 5개 | 2 |

### 3.4 페이지
A4 · 여백 1080 dxa · Times New Roman 10pt

---

## 4. 단어 배정

### 4.1 순서 — 까다로운 PART 먼저
```
IV → III → II → V → VI → I
```
PART I 은 다른 PART 가 쓰지 않는 **2단어 이상 어구**와 남은 단어로 채운다.

### 4.2 거르는 기준
| PART | 조건 |
| --- | --- |
| IV | `pos === '동'` |
| III | `derivatives.length >= 1` (파생 2문항), 유의어·반의어 각 1·2문항 |
| II | `pos` 가 형/부 — 품사로 답을 추론하지 못하게 통일 |
| V | `synonyms.length >= 2` (반의어 보유는 조건 아님) |
| VI | 제한 느슨 |
| I | `pos === '구'` 또는 `surface` 에 공백 포함 + 남은 단어 |

### 4.3 중복 금지
35개 정답에 중복 0건. distractor 로만 쓴 단어도 다른 PART 정답으로 쓰지 않는다.

> [duplicates.js](src/lib/duplicates.js) 의 `inflectionKey` 로 대장을 관리한다.
> 문자열 비교로는 `develop` / `developing` 이 다른 단어로 통과해버린다.

---

## 5. PART별 출제 규칙

### PART I — 우리말 → 영어 쓰기 (10문항)
- 어구를 넉넉히 넣는다 (샘플은 4개). 단어 수를 괄호로 표기: `~을 대신하여 (3)`
- 동사는 PART IV 에 양보
- 2열 5행 표: `No | 우리말 뜻 | 영단어` × 2

### PART II — 영영풀이 매칭 (5문항)
- **형용사·부사로 통일** (품사 추론 차단)
- 정의는 10~20단어로 풀어 쓴다. 유의어를 그대로 내놓지 않는다
- 보기 6개 중 1개는 distractor — 정답과 가깝지만 미묘하게 다른 말
- 한국어 번역은 넣지 않는다

### PART III — 어휘 관계 분석 (5문항)
- 파생 2 : 유의 1 : 반의 2
- 파생어 문항은 첫 글자를 주지 않는다. 유의어·반의어 문항은 첫 글자를 준다
- **어미 패턴 복제 차단** — 보기 쌍과 정답의 파생 어미가 달라야 한다 (파생어 문항만 해당)
- 단순 `-ly` 파생은 정답에서 뺀다. 같은 관계 유형을 연속 배치하지 않는다

샘플이 지킨 방식:
```
16. attain : attainment = comprehend : ______     (-ment → -sion)
17. intense : intensity = sufficient : ______     (-ity  → -ency)
18. hide : conceal = absorb : a______             유의어
19. present : absent = adjacent : r______         반의어
20. always : never = temporarily : p______        반의어
```

### PART IV — 동사 형태 변형 (5문항)
- 전부 동사. `rows[].sentence` 를 그대로 쓰고 동사 자리를 빈칸 처리
- 5문항의 형태가 서로 달라야 한다 — 3인칭 단수 / 동명사 / to부정사 / 과거분사 / 동사원형 / 현재분사 / 과거형
  - 원문에 다양한 형태가 없으면 **2개까지 같은 형태 허용**
- 보기 순서와 정답 순서를 다르게 섞는다

샘플이 지킨 방식:
```
보기 순서: commit · disrupt · attribute · induce · compensate
정답 순서: attributed(과거분사) · compensate(원형) · disrupts(3인칭) ·
          inducing(현재분사) · committing(동명사)
```

### PART V — 복수 정답 유의어 변별 (5문항)
- 표제어가 가장 자연스럽게 쓰이는 **문장을 새로 만든다** (지문에서 가져오지 않는다)
- 25~40단어, 학술적·추상적 주제. 5문항이 서로 다른 영역
- 정답 개수를 흩는다 — 샘플은 3·2·2·2·1
- 오답은 반대 방향 어휘. **쉬운 말은 쓰지 않는다** (weaken ✗ / undermine ○)
  - 단어장의 반의어를 우선 쓰되, 없으면 밖에서 가져와도 된다
- 문맥 단서를 2개 이상 심는다 — 부사, 결합 명사, 대조 표현, 시간·원인 표지

샘플 단서 예: `without a single objection` · `deliberately ambiguous` ·
`rather than treating them as separate tracks` · `acting entirely on their own initiative`

### PART VI — 지문형 빈칸 서사 (5문항)
- 90~110단어 인물 서사. 회차마다 다른 직업·배경, 보편적 가치(끈기·헌신·공동체)
- 빈칸 5개를 고르게 흩고, 보기 단어를 **변형 없이 원형 그대로** 넣는다
- 보기 순서와 정답 순서를 다르게 섞는다

샘플: `Dr. Osei` 영장류 연구자 서사 / 보기 `critically · neural · domain · indefinitely · variant`

---

## 6. 정답·해설지

| PART | 형식 | 출처 |
| --- | --- | --- |
| I | 표 `No / 정답 / 품사 / 뜻 / 유의어 / 출처` | **필요** |
| II | 카드: 정답 → 정의 풀이 → distractor 가 왜 아닌지 | 없음 |
| III | 카드: 정답 → 관계 유형 → 어간 분석 | 없음 |
| IV | 카드: 정답+형태 → 형태 근거 → 의미 → 문맥 단서 → 전문 번역 | **필요** |
| V | 카드: 정답 개수와 번호 → 오답 분석 → 문맥 단서 → 전문 번역 | 불가 |
| VI | 정답 5개 한 줄 → 문항별 품사·의미·단서 → 전체 한국어 해석(정답 굵게) | 불가 |

출처는 지문 번호(`passageNo`)로 적는다. 시험지 본문에는 출처를 넣지 않는다.
PART V·VI 는 문장을 새로 만들므로 출처가 존재하지 않는다.

색: 머리글 청남색 `#1F4E79` · 표 머리 옅은 청색 `#D5E8F0` · 정답과 해설지 머리글 레드 `#C00000`

---

## 7. 산출물

`docx` 패키지(v9.7.1)로 만든다.

| 파일 | 이름 |
| --- | --- |
| 시험지 | `{docTitle}_시험지.docx` |
| 정답·해설지 | `{docTitle}_정답해설.docx` |

---

## 8. 검증

- [ ] 정답 35개 중복 0건 (distractor 와 다른 PART 정답 간 중복 포함)
- [ ] PART II 보기 6개 중 1개 미사용
- [ ] PART IV 미사용 없음 · 형태가 서로 다름(같은 형태 최대 2) · 문장이 원문과 100% 일치
- [ ] PART V 정답 개수가 1~3개로 흩어져 있음
- [ ] PART VI 본문 100단어 내외, 빈칸 5개
- [ ] 보기 순서와 정답 순서가 다름 (PART IV·VI)
- [ ] 한글 폰트 깨짐 없음, 표 정렬 적절
